@model VidlyCore.Models.Customer
@{
    ViewData["Title"] = "Customer Details";
}
<h1>@Model.Name</h1>
<ul>
    <li><strong>Membership Type:</strong> @Model.MembershipType.Name</li>
    @if (Model.Birthdate != null)
    {
        <li><strong>Birthday:</strong> @Model.Birthdate</li>
    }

</ul>
