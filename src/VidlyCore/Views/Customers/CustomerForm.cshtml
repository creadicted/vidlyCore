@model VidlyCore.ViewModel.CustomerFormViewModel
@{
    ViewBag.Title = Model.Title;
}

<h2>@Model.Title</h2>

<div class="row">
    <div class="col-md-8">
        <section>
            <form asp-controller="Customers" asp-action="Save" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form-horizontal">
                
                <div asp-validation-summary="All" class="alert alert-warning">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    Please fix the errors below.
                </div>

                <div class="form-group">
                    <label asp-for="Name" class="col-md-2 control-label"></label>
                    <div class="col-md-10">
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger help-block"></span>
                    </div>
                </div>

                <div class="form-group">
                    <label asp-for="MembershipTypeId" class="col-md-2 control-label"></label>
                    <div class="col-md-10">

                        <select asp-for="MembershipTypeId"
                                asp-items="@(new SelectList(Model.MembershipTypes, "Id", "Name"))" class="form-control">
                            <option>Select Membership Type</option>
                        </select>
                        <span asp-validation-for="MembershipTypeId" asp-validat class="text-danger help-block"></span>
                    </div>
                </div>

                <div class="form-group">
                    <label asp-for="Birthdate" class="col-md-2 control-label"></label>
                    <div class="col-md-10">
                        <input asp-for="Birthdate" asp-format="{0:yyyy-MM-dd}" class="form-control" />
                        <span asp-validation-for="Birthdate" class="text-danger help-block"></span>
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <div class="checkbox">
                            <label asp-for="IsSubscribedToNewsletter">
                                <input asp-for="IsSubscribedToNewsletter" />
                                @Html.DisplayNameFor(m => m.IsSubscribedToNewsletter)
                            </label>
                        </div>
                    </div>
                </div>

                <input asp-for="Id" />

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <button type="submit" class="btn btn-primary">Save Customer</button>
                    </div>
                </div>
            </form>
        </section>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
